global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'post-analyzer-monitor'

scrape_configs:
  - job_name: 'post-analyzer'
    static_configs:
      - targets: ['app:8080']
    metrics_path: '/metrics'
    scrape_interval: 10s
