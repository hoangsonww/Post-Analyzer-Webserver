meta {
  name: Viewer cannot access admin status (expect 403)
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/admin/status
  body: none
  auth: inherit
}

tests {
  test("status 403", function() {
    expect(res.getStatus()).to.equal(403);
  });
}
