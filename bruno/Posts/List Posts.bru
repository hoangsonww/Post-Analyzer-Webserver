meta {
  name: List Posts
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/posts?page=1&pageSize=20
  body: none
  auth: inherit
}

tests {
  test("status 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
